<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tableau de bord administrateur - Yodes Hub</title>
  <style>
    :root {
      /* --- PALETTE DORÉE PROFESSIONNELLE --- */
      --primary: #d4af37;
      --primary-hover: #b8941f;
      --primary-light: #f5e6c3;
      --primary-dark: #9a7b1a;
      --success: #10b981;
      --success-light: #d1fae5;
      --success-dark: #059669;
      --warning: #f59e0b;
      --warning-light: #fef3c7;
      --danger: #ef4444;
      --danger-light: #fee2e2;
      --info: #d4af37;
      --info-light: #fef9e7;
      --gold: #d4af37;
      --gold-light: #fef9e7;
      --gold-dark: #b8941f;
      --bg-main: #fafaf8;
      --bg-secondary: #f5f1e8;
      --text-main: #1a1410;
      --text-secondary: #4a3f2e;
      --text-muted: #8a7d5c;
      --card-bg: #ffffff;
      --border: #e6dcc8;
      --border-light: #f5f1e8;
      --shadow-xs: 0 1px 2px rgba(212, 175, 55, 0.05);
      --shadow-sm: 0 2px 4px rgba(212, 175, 55, 0.08);
      --shadow-md: 0 4px 12px rgba(212, 175, 55, 0.12);
      --shadow-lg: 0 10px 25px rgba(212, 175, 55, 0.15);
      --shadow-xl: 0 20px 40px rgba(212, 175, 55, 0.2);
      --radius-sm: 8px;
      --radius-md: 12px;
      --radius-lg: 16px;
      --radius-xl: 20px;
      --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen', 'Ubuntu', 'Cantarell', sans-serif;
      background: linear-gradient(135deg, #fafaf8 0%, #f5e6c3 100%);
      color: var(--text-main);
      line-height: 1.6;
      min-height: 100vh;
      position: relative;
    }

    body::before {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      height: 500px;
      background: linear-gradient(135deg, #d4af37 0%, #f4e4b3 100%);
      opacity: 0.08;
      z-index: 0;
      pointer-events: none;
    }

    .dashboard-container {
      max-width: 1600px;
      margin: 0 auto;
      padding: 40px 24px;
      position: relative;
      z-index: 1;
    }

    /* Enhanced Header */
    .dashboard-header {
      margin-bottom: 48px;
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      flex-wrap: wrap;
      gap: 24px;
      animation: slideDown 0.6s ease-out;
    }

    @keyframes slideDown {
      from {
        opacity: 0;
        transform: translateY(-20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .header-content h1 {
      font-size: 2.75rem;
      font-weight: 900;
      background: linear-gradient(135deg, var(--primary) 0%, var(--gold-dark) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      margin: 0 0 12px 0;
      display: flex;
      align-items: center;
      gap: 16px;
      letter-spacing: -0.02em;
    }

    .header-emoji {
      font-size: 3rem;
      filter: drop-shadow(0 4px 12px rgba(212, 175, 55, 0.4));
      animation: float 3s ease-in-out infinite;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0px) rotate(0deg); }
      50% { transform: translateY(-10px) rotate(5deg); }
    }

    .header-content p {
      color: var(--text-secondary);
      font-size: 1.05rem;
      font-weight: 500;
    }

    .header-actions {
      display: flex;
      gap: 12px;
      flex-wrap: wrap;
    }

    /* Enhanced Buttons */
    .btn {
      padding: 14px 28px;
      border-radius: var(--radius-lg);
      font-weight: 600;
      font-size: 0.95rem;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      gap: 10px;
      transition: var(--transition);
      border: none;
      cursor: pointer;
      position: relative;
      overflow: hidden;
    }

    .btn::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255,255,255,0.4), transparent);
      transition: left 0.5s;
    }

    .btn:hover::before {
      left: 100%;
    }

    .btn-primary {
      background: linear-gradient(135deg, var(--primary) 0%, var(--gold-dark) 100%);
      color: white;
      box-shadow: 0 4px 12px rgba(212, 175, 55, 0.4);
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 20px rgba(212, 175, 55, 0.5);
    }

    .btn-secondary {
      background: white;
      color: var(--text-main);
      border: 2px solid var(--border);
      box-shadow: var(--shadow-sm);
    }

    .btn-secondary:hover {
      border-color: var(--primary);
      background: var(--primary-light);
      color: var(--primary-dark);
    }

    /* Enhanced Stats Grid */
    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 24px;
      margin-bottom: 48px;
    }

    .stat-card {
      background: linear-gradient(135deg, #ffffff 0%, #fffbf5 100%);
      border-radius: var(--radius-xl);
      padding: 32px;
      border: 2px solid var(--border-light);
      box-shadow: var(--shadow-md);
      transition: var(--transition);
      position: relative;
      overflow: hidden;
      animation: fadeInUp 0.6s ease-out backwards;
    }

    .stat-card:nth-child(1) { animation-delay: 0.1s; }
    .stat-card:nth-child(2) { animation-delay: 0.2s; }
    .stat-card:nth-child(3) { animation-delay: 0.3s; }
    .stat-card:nth-child(4) { animation-delay: 0.4s; }
    .stat-card:nth-child(5) { animation-delay: 0.5s; }
    .stat-card:nth-child(6) { animation-delay: 0.6s; }

    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .stat-card::before {
      content: '';
      position: absolute;
      top: -50%;
      right: -50%;
      width: 200%;
      height: 200%;
      background: radial-gradient(circle, rgba(212, 175, 55, 0.1) 0%, transparent 50%);
      transition: var(--transition);
      opacity: 0;
    }

    .stat-card:hover {
      transform: translateY(-8px) scale(1.02);
      box-shadow: 0 12px 30px rgba(212, 175, 55, 0.25);
      border-color: var(--primary);
    }

    .stat-card:hover::before {
      opacity: 1;
      top: -25%;
      right: -25%;
    }

    .stat-card.success::before { background: radial-gradient(circle, rgba(16, 185, 129, 0.1) 0%, transparent 50%); }
    .stat-card.warning::before { background: radial-gradient(circle, rgba(245, 158, 11, 0.1) 0%, transparent 50%); }
    .stat-card.danger::before { background: radial-gradient(circle, rgba(239, 68, 68, 0.1) 0%, transparent 50%); }
    .stat-card.info::before { background: radial-gradient(circle, rgba(212, 175, 55, 0.1) 0%, transparent 50%); }
    .stat-card.gold::before { background: radial-gradient(circle, rgba(212, 175, 55, 0.12) 0%, transparent 50%); }

    .stat-header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      margin-bottom: 24px;
      position: relative;
      z-index: 1;
    }

    .stat-icon {
      width: 64px;
      height: 64px;
      border-radius: var(--radius-lg);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 32px;
      background: linear-gradient(135deg, rgba(212, 175, 55, 0.15) 0%, rgba(212, 175, 55, 0.05) 100%);
      border: 2px solid rgba(212, 175, 55, 0.3);
      box-shadow: 0 4px 16px rgba(212, 175, 55, 0.2);
      transition: var(--transition);
    }

    .stat-card:hover .stat-icon {
      transform: scale(1.1) rotate(5deg);
      box-shadow: 0 8px 24px rgba(212, 175, 55, 0.35);
    }

    .stat-card.success .stat-icon { 
      background: linear-gradient(135deg, rgba(16, 185, 129, 0.15) 0%, rgba(16, 185, 129, 0.05) 100%);
      border-color: rgba(16, 185, 129, 0.3);
      box-shadow: 0 4px 16px rgba(16, 185, 129, 0.2);
    }
    
    .stat-card.warning .stat-icon { 
      background: linear-gradient(135deg, rgba(245, 158, 11, 0.15) 0%, rgba(245, 158, 11, 0.05) 100%);
      border-color: rgba(245, 158, 11, 0.3);
      box-shadow: 0 4px 16px rgba(245, 158, 11, 0.2);
    }
    
    .stat-card.danger .stat-icon { 
      background: linear-gradient(135deg, rgba(239, 68, 68, 0.15) 0%, rgba(239, 68, 68, 0.05) 100%);
      border-color: rgba(239, 68, 68, 0.3);
      box-shadow: 0 4px 16px rgba(239, 68, 68, 0.2);
    }
    
    .stat-card.info .stat-icon { 
      background: linear-gradient(135deg, rgba(212, 175, 55, 0.15) 0%, rgba(212, 175, 55, 0.05) 100%);
      border-color: rgba(212, 175, 55, 0.3);
      box-shadow: 0 4px 16px rgba(212, 175, 55, 0.2);
    }
    
    .stat-card.gold .stat-icon { 
      background: linear-gradient(135deg, rgba(212, 175, 55, 0.2) 0%, rgba(212, 175, 55, 0.08) 100%);
      border-color: rgba(212, 175, 55, 0.4);
      box-shadow: 0 4px 16px rgba(212, 175, 55, 0.25);
    }

    .stat-label {
      font-size: 0.8rem;
      font-weight: 700;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 0.08em;
      margin-bottom: 12px;
    }

    .stat-value {
      font-size: 3rem;
      font-weight: 900;
      color: var(--text-main);
      margin-bottom: 12px;
      line-height: 1;
      letter-spacing: -0.02em;
    }

    .stat-change {
      font-size: 0.9rem;
      color: var(--text-muted);
      display: flex;
      align-items: center;
      gap: 8px;
      font-weight: 600;
    }

    .stat-change.positive { 
      color: var(--success);
    }
    
    .stat-change.negative { 
      color: var(--danger); 
    }

    /* Enhanced Progress Bar */
    .stat-progress {
      margin-top: 20px;
      height: 8px;
      background: var(--border-light);
      border-radius: 10px;
      overflow: hidden;
      position: relative;
    }

    .stat-progress-bar {
      height: 100%;
      background: linear-gradient(90deg, var(--primary) 0%, var(--gold-dark) 100%);
      border-radius: 10px;
      transition: width 1s cubic-bezier(0.4, 0, 0.2, 1);
      position: relative;
      overflow: hidden;
    }

    .stat-progress-bar::after {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(90deg, transparent, rgba(255,255,255,0.4), transparent);
      animation: shimmer 2s infinite;
    }

    @keyframes shimmer {
      0% { transform: translateX(-100%); }
      100% { transform: translateX(100%); }
    }

    .stat-card.success .stat-progress-bar {
      background: linear-gradient(90deg, var(--success) 0%, var(--success-dark) 100%);
    }

    /* Enhanced Cards */
    .content-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(550px, 1fr));
      gap: 32px;
      margin-bottom: 48px;
    }

    .card {
      background: linear-gradient(135deg, #ffffff 0%, #fffbf5 100%);
      border-radius: var(--radius-xl);
      border: 2px solid var(--border-light);
      box-shadow: var(--shadow-md);
      overflow: hidden;
      transition: var(--transition);
      animation: fadeInUp 0.6s ease-out backwards;
    }

    .card:hover {
      box-shadow: 0 12px 30px rgba(212, 175, 55, 0.2);
      transform: translateY(-4px);
      border-color: var(--primary);
    }

    .card-header {
      padding: 28px 32px;
      border-bottom: 2px solid var(--border-light);
      background: linear-gradient(to bottom, #ffffff 0%, #fefdf9 100%);
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .card-header h2 {
      font-size: 1.35rem;
      font-weight: 800;
      color: var(--text-main);
      margin: 0;
      display: flex;
      align-items: center;
      gap: 12px;
    }

    .card-header-action {
      font-size: 0.9rem;
      color: var(--primary);
      text-decoration: none;
      font-weight: 700;
      transition: var(--transition);
      display: flex;
      align-items: center;
      gap: 6px;
    }

    .card-header-action:hover {
      color: var(--primary-hover);
      transform: translateX(4px);
    }

    .card-body {
      padding: 32px;
    }

    /* Enhanced Table */
    .data-table {
      width: 100%;
      border-collapse: collapse;
    }

    .data-table th {
      text-align: left;
      padding: 16px 14px;
      font-size: 0.75rem;
      font-weight: 800;
      text-transform: uppercase;
      letter-spacing: 0.08em;
      color: var(--text-muted);
      border-bottom: 2px solid var(--border);
      background: var(--bg-secondary);
    }

    .data-table td {
      padding: 18px 14px;
      border-bottom: 1px solid var(--border-light);
      font-size: 0.95rem;
    }

    .data-table tbody tr {
      transition: var(--transition);
    }

    .data-table tbody tr:hover {
      background: linear-gradient(to right, var(--primary-light) 0%, transparent 100%);
      transform: scale(1.01);
    }

    /* Enhanced Rank Badges */
    .rank-badge {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 44px;
      height: 44px;
      border-radius: var(--radius-md);
      font-weight: 800;
      font-size: 1.1rem;
      transition: var(--transition);
    }

    .rank-badge.gold {
      background: linear-gradient(135deg, #FFD700 0%, #d4af37 100%);
      color: #fff;
      box-shadow: 0 4px 16px rgba(255, 215, 0, 0.5);
      border: 2px solid #FFA500;
    }

    .rank-badge.silver {
      background: linear-gradient(135deg, #E8E8E8 0%, #C0C0C0 100%);
      color: #fff;
      box-shadow: 0 4px 16px rgba(192, 192, 192, 0.4);
      border: 2px solid #A8A8A8;
    }

    .rank-badge.bronze {
      background: linear-gradient(135deg, #CD7F32 0%, #A0522D 100%);
      color: #fff;
      box-shadow: 0 4px 16px rgba(205, 127, 50, 0.4);
      border: 2px solid #8B4513;
    }

    .rank-badge.default {
      background: var(--bg-secondary);
      color: var(--text-secondary);
      border: 2px solid var(--border);
      font-weight: 700;
    }

    .data-table tbody tr:hover .rank-badge {
      transform: scale(1.15) rotate(5deg);
    }

    /* Enhanced Badges */
    .badge {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 8px 16px;
      border-radius: 20px;
      font-size: 0.75rem;
      font-weight: 800;
      text-transform: uppercase;
      letter-spacing: 0.03em;
      transition: var(--transition);
    }

    .badge:hover {
      transform: scale(1.05);
    }

    .badge-success {
      background: linear-gradient(135deg, var(--success-light) 0%, #d1fae5 100%);
      color: #166534;
      box-shadow: 0 2px 8px rgba(16, 185, 129, 0.2);
      border: 1px solid #10b981;
    }

    .badge-warning {
      background: linear-gradient(135deg, var(--warning-light) 0%, #fef3c7 100%);
      color: #92400e;
      box-shadow: 0 2px 8px rgba(245, 158, 11, 0.2);
      border: 1px solid #f59e0b;
    }

    .badge-danger {
      background: linear-gradient(135deg, var(--danger-light) 0%, #fee2e2 100%);
      color: #991b1b;
      box-shadow: 0 2px 8px rgba(239, 68, 68, 0.2);
      border: 1px solid #ef4444;
    }

    .badge-info {
      background: linear-gradient(135deg, var(--info-light) 0%, #fef9e7 100%);
      color: #7d6e3b;
      box-shadow: 0 2px 8px rgba(212, 175, 55, 0.25);
      border: 1px solid #d4af37;
    }

    .badge-primary {
      background: linear-gradient(135deg, var(--primary-light) 0%, #f5e6c3 100%);
      color: #5a4a1f;
      box-shadow: 0 2px 8px rgba(212, 175, 55, 0.25);
      border: 1px solid #d4af37;
    }

    /* Enhanced Avatars */
    .avatar {
      width: 52px;
      height: 52px;
      border-radius: var(--radius-md);
      object-fit: cover;
      border: 3px solid var(--primary);
      box-shadow: 0 4px 12px rgba(212, 175, 55, 0.3);
      transition: var(--transition);
    }

    .avatar:hover {
      transform: scale(1.15) rotate(3deg);
      box-shadow: 0 6px 20px rgba(212, 175, 55, 0.4);
    }

    .avatar-placeholder {
      width: 52px;
      height: 52px;
      border-radius: var(--radius-md);
      background: linear-gradient(135deg, var(--primary) 0%, var(--gold-dark) 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-size: 1.1rem;
      font-weight: 800;
      border: 3px solid white;
      box-shadow: 0 4px 12px rgba(212, 175, 55, 0.35);
      transition: var(--transition);
    }

    .avatar-placeholder:hover {
      transform: scale(1.15) rotate(3deg);
      box-shadow: 0 6px 20px rgba(212, 175, 55, 0.5);
    }

    /* Enhanced Links */
    .link {
      color: var(--primary);
      text-decoration: none;
      font-weight: 700;
      transition: var(--transition);
      position: relative;
    }

    .link::after {
      content: '';
      position: absolute;
      bottom: -2px;
      left: 0;
      width: 0;
      height: 2px;
      background: linear-gradient(90deg, var(--primary) 0%, var(--gold-dark) 100%);
      transition: width 0.3s;
    }

    .link:hover {
      color: var(--primary-hover);
    }

    .link:hover::after {
      width: 100%;
    }

    /* Vote Trend */
    .vote-trend {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      font-size: 0.85rem;
      font-weight: 700;
      padding: 6px 14px;
      border-radius: 16px;
      transition: var(--transition);
    }

    .vote-trend:hover {
      transform: scale(1.08);
    }

    .vote-trend.up {
      background: linear-gradient(135deg, var(--success-light) 0%, #d1fae5 100%);
      color: var(--success-dark);
      box-shadow: 0 2px 8px rgba(16, 185, 129, 0.2);
      border: 1px solid #10b981;
    }

    .vote-trend.down {
      background: linear-gradient(135deg, var(--danger-light) 0%, #fee2e2 100%);
      color: var(--danger);
      box-shadow: 0 2px 8px rgba(239, 68, 68, 0.2);
      border: 1px solid #ef4444;
    }

    .vote-trend.neutral {
      background: var(--border-light);
      color: var(--text-muted);
      border: 1px solid var(--border);
    }

    /* Enhanced Empty State */
    .empty-state {
      text-align: center;
      padding: 80px 24px;
      color: var(--text-muted);
    }

    .empty-state-icon {
      font-size: 80px;
      margin-bottom: 24px;
      opacity: 0.3;
      filter: grayscale(1);
      animation: pulse 2s ease-in-out infinite;
    }

    @keyframes pulse {
      0%, 100% { opacity: 0.3; transform: scale(1); }
      50% { opacity: 0.5; transform: scale(1.05); }
    }

    .empty-state h3 {
      font-size: 1.5rem;
      font-weight: 700;
      margin-bottom: 12px;
      color: var(--text-main);
    }

    .empty-state p {
      font-size: 1rem;
      color: var(--text-muted);
    }

    /* Enhanced Quick Actions */
    .quick-actions-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: 20px;
    }

    .quick-action-card {
      padding: 32px;
      background: linear-gradient(135deg, #ffffff 0%, #fffbf5 100%);
      border-radius: var(--radius-xl);
      text-align: center;
      border: 2px solid var(--border-light);
      text-decoration: none;
      color: var(--text-main);
      transition: var(--transition);
      position: relative;
      overflow: hidden;
    }

    .quick-action-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 5px;
      background: linear-gradient(90deg, var(--primary) 0%, var(--gold-dark) 100%);
      transform: scaleX(0);
      transition: transform 0.4s;
    }

    .quick-action-card:hover {
      transform: translateY(-8px) scale(1.03);
      box-shadow: 0 12px 30px rgba(212, 175, 55, 0.25);
      border-color: var(--primary);
    }

    .quick-action-card:hover::before {
      transform: scaleX(1);
    }

    .quick-action-icon {
      font-size: 48px;
      margin-bottom: 16px;
      display: block;
      transition: var(--transition);
      filter: drop-shadow(0 2px 8px rgba(212, 175, 55, 0.2));
    }

    .quick-action-card:hover .quick-action-icon {
      transform: scale(1.2) rotate(5deg);
    }

    .quick-action-title {
      font-weight: 700;
      font-size: 1.05rem;
      color: var(--text-main);
    }

    .quick-action-card.featured {
      background: linear-gradient(135deg, var(--primary) 0%, var(--gold-dark) 100%);
      color: white;
      border: none;
      box-shadow: 0 8px 24px rgba(212, 175, 55, 0.4);
    }

    .quick-action-card.featured .quick-action-title {
      color: white;
    }

    .quick-action-card.featured::before {
      background: linear-gradient(90deg, rgba(255,255,255,0.4) 0%, rgba(255,255,255,0) 100%);
    }

    /* Summary Cards */
    .summary-row {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 20px;
      margin-top: 48px;
    }

    .summary-card {
      background: linear-gradient(135deg, white 0%, #fffbf5 100%);
      padding: 28px;
      border-radius: var(--radius-xl);
      border: 2px solid var(--border-light);
      text-align: center;
      transition: var(--transition);
      box-shadow: var(--shadow-sm);
    }

    .summary-card:hover {
      transform: translateY(-6px);
      box-shadow: 0 10px 25px rgba(212, 175, 55, 0.2);
      border-color: var(--primary);
    }

    .summary-card-value {
      font-size: 2.5rem;
      font-weight: 900;
      color: var(--text-main);
      margin-bottom: 8px;
      letter-spacing: -0.02em;
    }

    .summary-card-label {
      font-size: 0.8rem;
      color: var(--text-muted);
      text-transform: uppercase;
      font-weight: 700;
      letter-spacing: 0.08em;
    }

    /* Responsive Design */
    @media (max-width: 1200px) {
      .content-grid {
        grid-template-columns: 1fr;
      }
    }

    @media (max-width: 768px) {
      .dashboard-container {
        padding: 24px 16px;
      }

      .dashboard-header {
        flex-direction: column;
      }

      .header-content h1 {
        font-size: 2rem;
      }

      .stats-grid {
        grid-template-columns: 1fr;
      }

      .stat-value {
        font-size: 2.5rem;
      }

      .quick-actions-grid {
        grid-template-columns: 1fr;
      }
    }

    /* Utility Classes */
    .text-muted {
      color: var(--text-muted);
      font-size: 0.9rem;
    }

    /* Decorative Elements */
    .decorative-shine {
      position: absolute;
      top: 10px;
      right: 10px;
      width: 40px;
      height: 40px;
      background: radial-gradient(circle, rgba(212, 175, 55, 0.3) 0%, transparent 70%);
      border-radius: 50%;
      pointer-events: none;
      animation: shine 3s ease-in-out infinite;
    }

    @keyframes shine {
      0%, 100% { opacity: 0.3; transform: scale(1); }
      50% { opacity: 0.6; transform: scale(1.2); }
    }
  </style>
</head>
<body>
  <div class="dashboard-container">
    <!-- Header Section -->
    <div class="dashboard-header">
      <div class="header-content">
        <h1>
          <span class="header-emoji">🏆</span>
          Tableau de bord administrateur
        </h1>
        <p>Vue d'ensemble complète de votre plateforme Yodes Hub • Dernière mise à jour: <%= Time.now.strftime("%d/%m/%Y à %H:%M") %></p>
      </div>
      <div class="header-actions">
        <a href="<%= admin_candidates_path %>" class="btn btn-secondary">
          <span>👥</span>
          <span>Candidats</span>
        </a>
        <a href="<%= new_admin_candidate_path %>" class="btn btn-primary">
          <span>➕</span>
          <span>Nouveau candidat</span>
        </a>
      </div>
    </div>

    <!-- Statistics Cards -->
    <div class="stats-grid">
      <!-- Total Candidates -->
      <div class="stat-card">
        <div class="decorative-shine"></div>
        <div class="stat-header">
          <div style="flex: 1;">
            <div class="stat-label">Total Candidats</div>
            <div class="stat-value"><%= @total_candidates %></div>
            <div class="stat-change">
              <span>✓</span>
              <span><%= @active_candidates %> actifs</span>
            </div>
          </div>
          <div class="stat-icon">👥</div>
        </div>
        <div class="stat-progress">
          <div class="stat-progress-bar" style="width: <%= @total_candidates > 0 ? (@active_candidates.to_f / @total_candidates * 100).round : 0 %>%;"></div>
        </div>
      </div>

      <!-- Total Votes -->
      <div class="stat-card success">
        <div class="decorative-shine"></div>
        <div class="stat-header">
          <div style="flex: 1;">
            <div class="stat-label">Total Votes</div>
            <div class="stat-value"><%= number_with_delimiter(@total_votes) %></div>
            <div class="stat-change positive">
              <span>↗</span>
              <span><%= @votes_today %> aujourd'hui</span>
            </div>
          </div>
          <div class="stat-icon">🗳️</div>
        </div>
        <div class="stat-progress">
          <div class="stat-progress-bar" style="width: <%= @total_votes > 0 ? [(@votes_today.to_f / @total_votes * 100).round, 100].min : 0 %>%;"></div>
        </div>
      </div>

      <!-- Users -->
      <div class="stat-card info">
        <div class="decorative-shine"></div>
        <div class="stat-header">
          <div style="flex: 1;">
            <div class="stat-label">Utilisateurs Inscrits</div>
            <div class="stat-value"><%= number_with_delimiter(@total_users) %></div>
            <div class="stat-change">
              <span>👤</span>
              <span>Membres actifs</span>
            </div>
          </div>
          <div class="stat-icon">👤</div>
        </div>
      </div>

      <!-- Videos -->
      <div class="stat-card gold">
        <div class="decorative-shine"></div>
        <div class="stat-header">
          <div style="flex: 1;">
            <div class="stat-label">Bibliothèque Vidéos</div>
            <div class="stat-value"><%= @total_videos %></div>
            <div class="stat-change">
              <span>🎥</span>
              <span><%= @candidate_videos %> candidats • <%= @emission_videos %> émissions</span>
            </div>
          </div>
          <div class="stat-icon">🎥</div>
        </div>
      </div>

      <!-- Weekly Votes -->
      <div class="stat-card warning">
        <div class="decorative-shine"></div>
        <div class="stat-header">
          <div style="flex: 1;">
            <div class="stat-label">Votes (7 derniers jours)</div>
            <div class="stat-value"><%= number_with_delimiter(@recent_votes) %></div>
            <% 
              weekly_growth = @total_votes > 0 ? ((@recent_votes.to_f / @total_votes) * 100).round(1) : 0
            %>
            <div class="stat-change <%= weekly_growth > 0 ? 'positive' : 'neutral' %>">
              <span><%= weekly_growth > 0 ? '↗' : '→' %></span>
              <span><%= weekly_growth %>% du total</span>
            </div>
          </div>
          <div class="stat-icon">📈</div>
        </div>
      </div>

      <!-- Top Candidate -->
      <div class="stat-card danger">
        <div class="decorative-shine"></div>
        <div class="stat-header">
          <div style="flex: 1;">
            <div class="stat-label">Candidat Plus de votes</div>
            <% if @top_candidates.first %>
              <div class="stat-value" style="font-size: 1.5rem;"><%= @top_candidates.first.name.split.map(&:first).join %></div>
              <div class="stat-change positive">
                <span>🏆</span>
                <span><%= number_with_delimiter(@top_candidates.first.votes_count) %> votes</span>
              </div>
            <% else %>
              <div class="stat-value">N/A</div>
            <% end %>
          </div>
          <div class="stat-icon">💯</div>
        </div>
        <% if @top_candidates.first && @total_votes > 0 %>
          <div class="stat-progress">
            <div class="stat-progress-bar" style="width: <%= [(@top_candidates.first.votes_count.to_f / @total_votes * 100).round, 100].min %>%; background: linear-gradient(90deg, var(--danger) 0%, #dc2626 100%);"></div>
          </div>
        <% end %>
      </div>
    </div>

    <!-- Content Grid -->
    <div class="content-grid">
      <!-- Top Candidates -->
      <div class="card">
        <div class="card-header">
          <h2>🏆 Top Candidats du Classement</h2>
          <a href="<%= admin_candidates_path %>" class="card-header-action">Voir tous →</a>
        </div>
        <div class="card-body">
          <% if @top_candidates.any? %>
            <table class="data-table">
              <thead>
                <tr>
                  <th>Rang</th>
                  <th>Candidat</th>
                  <th>Votes</th>
                  <th>Statut</th>
                </tr>
              </thead>
              <tbody>
                <% @top_candidates.each_with_index do |candidate, index| %>
                  <tr>
                    <td>
                      <% if index == 0 %>
                        <div class="rank-badge gold">🥇</div>
                      <% elsif index == 1 %>
                        <div class="rank-badge silver">🥈</div>
                      <% elsif index == 2 %>
                        <div class="rank-badge bronze">🥉</div>
                      <% else %>
                        <div class="rank-badge default"><%= index + 1 %></div>
                      <% end %>
                    </td>
                    <td>
                      <div style="display: flex; align-items: center; gap: 14px;">
                        <% if candidate.image.attached? %>
                          <%= image_tag candidate.image, class: "avatar", alt: candidate.name %>
                        <% else %>
                          <div class="avatar-placeholder"><%= candidate.name.split.map(&:first).join %></div>
                        <% end %>
                        <div>
                          <div style="font-weight: 700;">
                            <a href="<%= admin_candidate_path(candidate) %>" class="link"><%= candidate.name %></a>
                          </div>
                          <div class="text-muted"><%= candidate.name %></div>
                        </div>
                      </div>
                    </td>
                    <td>
                      <strong style="color: var(--gold); font-size: 1.2rem;"><%= number_with_delimiter(candidate.votes_count) %></strong>
                    </td>
                    <td>
                      <% if candidate.active %>
                        <span class="badge badge-success">✓ Actif</span>
                      <% else %>
                        <span class="badge badge-warning">⏸ Inactif</span>
                      <% end %>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          <% else %>
            <div class="empty-state">
              <div class="empty-state-icon">📊</div>
              <h3>Aucun candidat</h3>
              <p>Commencez par ajouter des candidats pour voir les statistiques</p>
            </div>
          <% end %>
        </div>
      </div>

      <!-- Recent Votes Activity -->
      <div class="card">
        <div class="card-header">
          <h2>🔄 Activité Récente des Votes</h2>
          <a href="#" class="card-header-action">Voir tous →</a>
        </div>
        <div class="card-body">
          <% if @latest_votes.any? %>
            <table class="data-table">
              <thead>
                <tr>
                  <th>Candidat</th>
                  <th>Utilisateur</th>
                  <th>Date & Heure</th>
                </tr>
              </thead>
              <tbody>
                <% @latest_votes.each do |vote| %>
                  <tr>
                    <td>
                      <div style="display: flex; align-items: center; gap: 10px;">
                        <% if vote.candidate.image.attached? %>
                          <%= image_tag vote.candidate.image, class: "avatar", style: "width: 36px; height: 36px;", alt: vote.candidate.name %>
                        <% else %>
                          <div class="avatar-placeholder" style="width: 36px; height: 36px; font-size: 0.8rem;">
                            <%= vote.candidate.name.split.map(&:first).join %>
                          </div>
                        <% end %>
                        <a href="<%= admin_candidate_path(vote.candidate) %>" class="link"><%= vote.candidate.name %></a>
                      </div>
                    </td>
                    <td class="text-muted">
                       'Utilisateur anonyme' 
                    </td>
                    <td class="text-muted">
                      <%= vote.created_at.strftime("%d/%m/%Y") %>
                      <span style="color: var(--primary); font-weight: 700; margin-left: 8px;">
                        <%= vote.created_at.strftime("%H:%M") %>
                      </span>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          <% else %>
            <div class="empty-state">
              <div class="empty-state-icon">🗳️</div>
              <h3>Aucun vote récent</h3>
              <p>Les votes apparaîtront ici dès qu'ils seront enregistrés</p>
            </div>
          <% end %>
        </div>
      </div>
    </div>

    <!-- Second Row -->
    <div class="content-grid">
      <!-- Recent Candidates -->
      <div class="card">
        <div class="card-header">
          <h2>✨ Candidats Récemment Ajoutés</h2>
          <a href="<%= admin_candidates_path %>" class="card-header-action">Voir tous →</a>
        </div>
        <div class="card-body">
          <% if @recent_candidates.any? %>
            <table class="data-table">
              <thead>
                <tr>
                  <th>Candidat</th>
                  <th>Votes</th>
                  <th>Statut</th>
                  <th>Ajouté</th>
                </tr>
              </thead>
              <tbody>
                <% @recent_candidates.each do |candidate| %>
                  <tr>
                    <td>
                      <div style="display: flex; align-items: center; gap: 14px;">
                        <% if candidate.image.attached? %>
                          <%= image_tag candidate.image, class: "avatar", alt: candidate.name %>
                        <% else %>
                          <div class="avatar-placeholder"><%= candidate.name.split.map(&:first).join %></div>
                        <% end %>
                        <div>
                          <a href="<%= admin_candidate_path(candidate) %>" class="link"><%= candidate.name %></a>
                          <% if candidate.created_at > 24.hours.ago %>
                            <span class="badge badge-info" style="margin-left: 10px; font-size: 0.65rem;">Nouveau</span>
                          <% end %>
                        </div>
                      </div>
                    </td>
                    <td>
                      <strong style="color: var(--gold);"><%= number_with_delimiter(candidate.votes.count) %></strong>
                    </td>
                    <td>
                      <% if candidate.active %>
                        <span class="badge badge-success">✓ Actif</span>
                      <% else %>
                        <span class="badge badge-warning">⏸ Inactif</span>
                      <% end %>
                    </td>
                    <td class="text-muted"><%= time_ago_in_words(candidate.created_at) %></td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          <% else %>
            <div class="empty-state">
              <div class="empty-state-icon">👥</div>
              <h3>Aucun candidat récent</h3>
              <p>Ajoutez votre premier candidat pour commencer</p>
            </div>
          <% end %>
        </div>
      </div>

      <!-- Recent Users -->
      <div class="card">
        <div class="card-header">
          <h2>👥 Nouveaux Utilisateurs</h2>
          <a href="#" class="card-header-action">Voir tous →</a>
        </div>
        <div class="card-body">
          <% if @recent_users.any? %>
            <table class="data-table">
              <thead>
                <tr>
                  <th>Email</th>
                  <th>Votes</th>
                  <th>Inscription</th>
                </tr>
              </thead>
              <tbody>
                <% @recent_users.each_with_index do |user, index| %>
                  <tr>
                    <td>
                      <div style="display: flex; align-items: center; gap: 14px;">
                        <% colors = [
                          "linear-gradient(135deg, var(--info-light) 0%, var(--border) 100%); color: var(--primary)",
                          "linear-gradient(135deg, var(--success-light) 0%, var(--border) 100%); color: var(--success)",
                          "linear-gradient(135deg, var(--warning-light) 0%, var(--border) 100%); color: var(--warning)",
                          "linear-gradient(135deg, var(--danger-light) 0%, var(--border) 100%); color: var(--danger)"
                        ] %>
                        <div class="avatar-placeholder" style="background: <%= colors[index % colors.length] %>">
                          <%= user.email.first.upcase %>
                        </div>
                        <a href="#" class="link"><%= user.email %></a>
                      </div>
                    </td>
                   
                    <td class="text-muted"><%= time_ago_in_words(user.created_at) %></td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          <% else %>
            <div class="empty-state">
              <div class="empty-state-icon">👤</div>
              <h3>Aucun utilisateur récent</h3>
              <p>Les nouveaux utilisateurs apparaîtront ici</p>
            </div>
          <% end %>
        </div>
      </div>
    </div>

    <!-- Quick Actions -->
    <div class="card" style="margin-top: 40px;">
      <div class="card-header">
        <h2>⚡ Actions Rapides</h2>
      </div>
      <div class="card-body">
        <div class="quick-actions-grid">
          <a href="<%= admin_candidates_path %>" class="quick-action-card">
            <span class="quick-action-icon">👥</span>
            <div class="quick-action-title">Gérer les candidats</div>
          </a>
          
          <a href="#" class="quick-action-card">
            <span class="quick-action-icon">🎥</span>
            <div class="quick-action-title">Gérer les vidéos</div>
          </a>
          
          <a href="#" class="quick-action-card">
            <span class="quick-action-icon">🗳️</span>
            <div class="quick-action-title">Voir les votes</div>
          </a>
          
          <a href="#" class="quick-action-card">
            <span class="quick-action-icon">👤</span>
            <div class="quick-action-title">Gérer les utilisateurs</div>
          </a>
          
          <a href="<%= new_admin_candidate_path %>" class="quick-action-card featured">
            <span class="quick-action-icon">➕</span>
            <div class="quick-action-title">Nouveau candidat</div>
          </a>

          <a href="#" class="quick-action-card">
            <span class="quick-action-icon">📹</span>
            <div class="quick-action-title">Nouvelle vidéo</div>
          </a>
        </div>
      </div>
    </div>

    <!-- System Summary -->
    <div class="summary-row">
      <div class="summary-card">
        <div class="summary-card-value" style="color: var(--gold);"><%= number_with_delimiter(@total_votes) %></div>
        <div class="summary-card-label">Total des Votes</div>
      </div>
      <div class="summary-card">
        <div class="summary-card-value" style="color: var(--primary);"><%= @active_candidates %></div>
        <div class="summary-card-label">Candidats Actifs</div>
      </div>
      <div class="summary-card">
        <div class="summary-card-value" style="color: var(--gold-dark);"><%= number_with_delimiter(@total_users) %></div>
        <div class="summary-card-label">Utilisateurs Total</div>
      </div>
      <div class="summary-card">
        <div class="summary-card-value" style="color: var(--primary);">
          <%= @total_users > 0 ? (@total_votes.to_f / @total_users).round(1) : '0.0' %>
        </div>
        <div class="summary-card-label">Votes / Utilisateur</div>
      </div>
    </div>
  </div>
</body>
</html>
