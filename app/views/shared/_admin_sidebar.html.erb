<style>
:root {
  /* --- PALETTE PROFESSIONNELLE "GOLDEN DARK" --- */
  --sidebar-bg: #1a1410;         /* Fond brun très profond */
  --sidebar-hover: #2d2416;      /* Survol chocolat chaud */
  
  /* Couleurs d'accentuation dorée */
  --sidebar-accent: #d4af37;     /* Or classique */
  --sidebar-accent-dark: #b8941f; /* Or foncé */
  --sidebar-active-bg: rgba(212, 175, 55, 0.15); /* Fond doré subtil pour l'actif */
  
  /* Typographie */
  --sidebar-text: #a89968;       /* Texte secondaire (beige doré) */
  --sidebar-text-bright: #f5f1e8; /* Texte principal (crème) */
  
  /* Dimensions et effets */
  --sidebar-width: 280px;
  --transition-smooth: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
}

.admin-sidebar {
  width: var(--sidebar-width);
  height: 100vh;
  background: linear-gradient(180deg, #1a1410 0%, #0f0c08 100%);
  color: var(--sidebar-text);
  display: flex;
  flex-direction: column;
  position: fixed;
  left: 0;
  top: 0;
  transition: var(--transition-smooth);
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
  border-right: 2px solid rgba(212, 175, 55, 0.2);
  box-shadow: 4px 0 20px rgba(212, 175, 55, 0.1);
  z-index: 1000;
}

/* --- Header & Brand --- */
.sidebar-header {
  padding: 35px 25px;
  border-bottom: 1px solid rgba(212, 175, 55, 0.15);
}

.brand {
  display: flex;
  align-items: center;
  gap: 14px;
}

.brand-logo {
  background: linear-gradient(135deg, var(--sidebar-accent), var(--sidebar-accent-dark));
  color: #1a1410;
  width: 40px;
  height: 40px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 12px;
  font-weight: 900;
  font-size: 1.3rem;
  box-shadow: 0 6px 20px rgba(212, 175, 55, 0.4);
  border: 2px solid rgba(212, 175, 55, 0.3);
}

.brand-name {
  margin: 0;
  font-size: 1.3rem;
  font-weight: 800;
  background: linear-gradient(135deg, var(--sidebar-accent), #f4e4b3);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  letter-spacing: -0.5px;
}

.brand-name span {
  font-weight: 400;
  background: linear-gradient(135deg, var(--sidebar-text), #8a7d5c);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

/* --- Navigation --- */
.admin-nav {
  flex: 1;
  padding: 0 16px;
  overflow-y: auto;
  scrollbar-width: thin;
  scrollbar-color: var(--sidebar-accent) transparent;
}

.admin-nav::-webkit-scrollbar {
  width: 6px;
}

.admin-nav::-webkit-scrollbar-track {
  background: transparent;
}

.admin-nav::-webkit-scrollbar-thumb {
  background: var(--sidebar-accent);
  border-radius: 10px;
}

.nav-section {
  margin-bottom: 32px;
}

.nav-label {
  display: block;
  padding: 0 12px 12px;
  font-size: 0.7rem;
  text-transform: uppercase;
  letter-spacing: 1.8px;
  color: #6d5d3a;
  font-weight: 800;
}

.admin-nav ul {
  list-style: none;
  padding: 0;
  margin: 0;
}

.admin-nav li {
  margin-bottom: 6px;
}

.admin-nav a {
  display: flex;
  align-items: center;
  padding: 12px 16px;
  color: var(--sidebar-text);
  text-decoration: none;
  border-radius: 10px;
  transition: var(--transition-smooth);
  font-weight: 500;
  position: relative;
  border: 1px solid transparent;
}

/* État Survol */
.admin-nav a:hover {
  background: linear-gradient(90deg, var(--sidebar-hover), rgba(212, 175, 55, 0.08));
  color: var(--sidebar-text-bright);
  border-color: rgba(212, 175, 55, 0.3);
  transform: translateX(4px);
}

/* --- État Actif Professionnel --- */
.admin-nav li.active a {
  background: linear-gradient(90deg, var(--sidebar-active-bg), rgba(212, 175, 55, 0.05));
  color: var(--sidebar-accent);
  font-weight: 700;
  border-color: rgba(212, 175, 55, 0.4);
  box-shadow: 0 4px 12px rgba(212, 175, 55, 0.2);
}

/* La barre verticale dorée à gauche de l'élément actif */
.admin-nav li.active a::before {
  content: '';
  position: absolute;
  left: -16px;
  top: 50%;
  transform: translateY(-50%);
  height: 28px;
  width: 4px;
  background: linear-gradient(180deg, var(--sidebar-accent), var(--sidebar-accent-dark));
  border-radius: 0 4px 4px 0;
  box-shadow: 0 0 10px rgba(212, 175, 55, 0.6);
}

/* Indicateur droit subtil pour l'actif */
.admin-nav li.active a::after {
  content: '◆';
  position: absolute;
  right: 16px;
  color: var(--sidebar-accent);
  font-size: 0.7rem;
  animation: pulse 2s ease-in-out infinite;
}

@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.4; }
}

/* Gestion des icônes (Emojis) */
.admin-nav .icon {
  margin-right: 14px;
  font-size: 1.3rem;
  filter: grayscale(0.8) sepia(1) hue-rotate(15deg) saturate(0.7);
  transition: var(--transition-smooth);
}

/* On redonne des couleurs dorées aux emojis au survol ou si actif */
.admin-nav a:hover .icon {
  filter: grayscale(0) brightness(1.2);
  transform: scale(1.1);
}

.admin-nav li.active .icon {
  filter: grayscale(0) brightness(1.3) drop-shadow(0 0 8px rgba(212, 175, 55, 0.6));
  transform: scale(1.1);
}

/* --- Footer --- */
.sidebar-footer {
  padding: 24px 16px;
  background: linear-gradient(180deg, rgba(212, 175, 55, 0.05), rgba(0, 0, 0, 0.2));
  border-top: 2px solid rgba(212, 175, 55, 0.2);
}

.back-link {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 12px 16px;
  color: var(--sidebar-text);
  text-decoration: none;
  border-radius: 10px;
  font-weight: 700;
  font-size: 0.95rem;
  transition: var(--transition-smooth);
  border: 2px solid rgba(212, 175, 55, 0.3);
  background: rgba(212, 175, 55, 0.05);
}

.back-link:hover {
  background: linear-gradient(135deg, var(--sidebar-accent), var(--sidebar-accent-dark));
  color: #1a1410;
  border-color: var(--sidebar-accent);
  box-shadow: 0 4px 16px rgba(212, 175, 55, 0.4);
  transform: translateY(-2px);
}

.back-link .text {
  position: relative;
}

.back-link:hover .text::before {
  content: '← ';
  position: absolute;
  left: -20px;
  animation: slideLeft 0.8s ease-in-out infinite;
}

@keyframes slideLeft {
  0%, 100% { transform: translateX(0); }
  50% { transform: translateX(-4px); }
}
</style>

<aside class="admin-sidebar" id="adminSidebar">
  <div class="sidebar-header">
    <div class="brand">
      <div class="brand-logo">Y</div>
      <h2 class="brand-name">Yodes<span>Hub</span></h2>
    </div>
  </div>

  <nav class="admin-nav">
    <div class="nav-section">
      <span class="nav-label">Menu Principal</span>
      <ul>
        <li class="<%= 'active' if current_page?(admin_dashboard_index_path) %>">
          <%= link_to admin_dashboard_index_path do %>
            <span class="icon">📊</span> <span class="text">Tableau de bord</span>
          <% end %>
         <li class="<%= 'active' if current_page?(admin_users_path) %>">
          <%= link_to admin_users_path do %>
            <span class="icon">👤</span> <span class="text">Utilisateurs</span>
          <% end %>
        <li class="<%= 'active' if current_page?(admin_candidates_path) %>">
          <%= link_to admin_candidates_path do %>
            <span class="icon">👥</span> <span class="text">Candidats</span>
          <% end %>
        </li>
        <li class="<%= 'active' if current_page?(admin_videos_path) %>">
          <%= link_to admin_videos_path do %>
            <span class="icon">🎥</span> <span class="text">Vidéos</span>
          <% end %>
        </li>
         <li class="<%= 'active' if current_page?(admin_partenaires_path) %>">
          <%= link_to admin_partenaires_path do %>
            <span class="icon">🤝</span> <span class="text">Partenaires</span>
          <% end %>
        </li>
        <li class="<%= 'active' if current_page?(admin_temoignages_path) %>">
          <%= link_to admin_temoignages_path do %>
            <span class="icon">💬</span> <span class="text">Témoignages</span>
          <% end %>
        </li>
      </ul>
    </div>

    <div class="nav-section">
      <span class="nav-label">Analyses</span>
      <ul>
        <li class="<%= 'active' if current_page?(admin_votes_path) %>">
          <%= link_to admin_votes_path do %>
            <span class="icon">🗳️</span> <span class="text">Votes</span>
          <% end %>
        </li>
        <li class="<%= 'active' if current_page?(admin_results_path) %>">
          <%= link_to admin_results_path do %>
            <span class="icon">📈</span> <span class="text">Résultats</span>
          <% end %>
        </li>
        
      </ul>
    </div>
  </nav>

  <div class="sidebar-footer">
    <%= link_to root_path, class: "back-link", target: '_blank' do %>
      <span class="text">Retour au site</span>
    <% end %>
  </div>
</aside>